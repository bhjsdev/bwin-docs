import IFrame from '@/components/iframe';

export const metadata = {
  title: 'Remove pane - JavaScript APIs - Binary Window',
};

# Remove a pane

## `removePane`

Remove a pane from the window.

```js
binaryWindow.removePane(paneId);
```

### Parameters

#### Main

| Name     | Type   | Description                  |
| -------- | ------ | ---------------------------- |
| `paneId` | string | ID of the pane to be removed |

### Returns

**Type**: `void`

### Example

<br />
<IFrame path="remove-pane" />

```html
<button id="remove-pane">Remove Pane 2</button>
<div id="container" style="width: 400px; height: 260px"></div>
```

```js
const bwin = new BinaryWindow({
  fitContainer: true,
  children: [
    { position: 'left', id: 'pane-1', content: 'Pane 1' },
    {
      position: 'right',
      id: 'pane-2',
      content: 'Pane 2',
    },
  ],
});

bwin.mount(document.getElementById('container'));

document.getElementById('remove-pane').addEventListener('click', () => {
  bwin.removePane('pane-2');
});
```
