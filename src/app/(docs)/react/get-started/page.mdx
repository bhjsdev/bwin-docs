import Client from '@/components/client';
import Example from '@/examples/react-get-started';
import { getPath } from '@/utils';

export const metadata = {
  title: 'Get started - React - Binary Window',
};

# React

Use BinaryWindow with React.

## Install with npm

Assume you have a React project set up.

```bash
npm install react-bwin
```

## Basic example

<Client>
  <Example />
</Client>

<br />

```jsx
import { Window } from 'react-bwin';
import 'react-bwin/react-bwin.css';

function Example() {
  return (
    <div style={{ width: 400, height: 300 }}>
      <Window
        fitContainer
        panes={[
          {
            position: 'left',
            size: 200,
            content: <em>Hello World</em>,
          },
          {
            position: 'right',
            children: [
              {
                position: 'top',
                size: '40%',
              },
              {
                position: 'bottom',
                size: '60%',
              },
            ],
          },
        ]}
      />
    </div>
  );
}
```

## Next steps

- <a href={getPath('/general/config')}>Config</a> — how the `panes` layout is described
- <a href={getPath('/react/add-pane')}>Add</a>, <a href={getPath('/react/update-pane')}>update</a>, and <a href={getPath('/react/remove-pane')}>remove</a> panes
- <a href={getPath('/react/use-window')}>useWindow hook</a> — reach the window API from any component
- <a href={getPath('/react/actions')}>Customize actions</a> and set a <a href={getPath('/react/theme')}>theme</a>
- <a href={getPath('/react/troubleshooting')}>Troubleshooting</a> — StrictMode and SSR hydration
